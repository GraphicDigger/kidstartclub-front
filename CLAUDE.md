# CLAUDE.md

## Язык общения

Всегда отвечай на русском языке, независимо от языка вопроса.

## Правила и принципы

Перед любой работой с кодом ОБЯЗАТЕЛЬНО прочитай `.specify/memory/constitution.md` — там описаны все архитектурные принципы, соглашения и критерии качества.

## Команды

```bash
npm run dev            # Дев-сервер
npm run build          # Продакшн-сборка
npm run lint           # ESLint
npm run typecheck      # TypeScript проверка (tsc --noEmit)
npm run storybook      # Storybook на порту 6006
npm run gen            # Plop-генераторы (сущности и т.д.)
```

## Алиас

`@/*` → `./src/*`

