import type { EntityState } from '@reduxjs/toolkit';

// Entity Schema
export interface {{titleCase name}} {
    id: string;
    name: string;
}

// Redux State Schemas

// Состояние UI-взаимодействий (фокус, выбор и т.д.)
export interface {{titleCase name}}UIState {
    hoveredId: string | null;
    focusedId: string | null;
    selectedId: string | null;
}

// Объединяет в себе нормализованные данные и состояние UI
export interface {{titleCase name}}State extends EntityState<{{titleCase name}}, string>, {{titleCase name}}UIState {}

// Полезная нагрузка для экшенов, работающих с ID
export interface {{titleCase name}}IdPayload {
    id: string | null;
} 